export type PieDataTabsListProps = {
    pieDataTabs: any[],
    onTabChange: (tab: any) => void,
    activePieDataTab: {}
}

export type SecondaryTabSwitchProps = {
    tabs: any[],
    onTabChange: (tab: any) => void,
    activeTab: {}
}